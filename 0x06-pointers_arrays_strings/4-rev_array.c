#include "main.h"
/**
 * reverse_array - reverse an array of string
 * @a: array
 * @n: size of aaray
 */
void reverse_array(int *a, int n)
{
		int i;

		for (i -= n; i >= 0; i--)
		{
			_putchar(i);
			_putchar(',');
		}
		_putchar(30);
}
